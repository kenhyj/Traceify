import React from 'react';
import { withRouter } from 'react-router-dom';

const FinancialHelp = () => {
    return (
        <div>
            This should be the Financial Help page.
        </div>
    );
};

export default withRouter(FinancialHelp);
