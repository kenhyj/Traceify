import React, { Component } from 'react';

class MapContainer extends Component {
    render() {
        return(
            <div>
                --- map component here ---
            </div>
        )};
}

export default MapContainer;